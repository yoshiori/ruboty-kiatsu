require 'spec_helper'

describe Ruboty::Kiatsu do
  it 'has a version number' do
    expect(Ruboty::Kiatsu::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
